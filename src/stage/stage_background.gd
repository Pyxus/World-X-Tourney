extends Spatial


func update(fighter_center: Vector3) -> void:
	_update_impl(fighter_center)
	
	
func _update_impl(_fighter_center: Vector3) -> void:
	#var distance := 60
	#var circumference := 2 * PI * distance
	#var t = fmod(-fighter_center.x / circumference, 2 * PI)
	pass
